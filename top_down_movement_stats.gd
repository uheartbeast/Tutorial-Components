class_name TopDownMovementStats
extends Resource

# For this simple top down movement our stats resource will only need
# 3 stat values (max speed, acceleration, and friction)
@export var max_speed = 100.0
@export var acceleration = 1000.0
@export var friction = 500.0
